(ocamllex
  (modules lexer))

(menhir
  (modules parser))

(library
  (name miniml)
  (modules
    ast
    core
    lexer
    optim
    parser
    repl
    types
    utils
    value))

(executable
  (name main)
  (modules main)
  (libraries miniml))
