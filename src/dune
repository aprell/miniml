(ocamllex
  (modules lexer))

(menhir
  (modules parser))

(library
  (name miniml)
  (modules lexer parser ast types core optim repl utils))

(executable
  (name main)
  (modules main)
  (libraries miniml))
